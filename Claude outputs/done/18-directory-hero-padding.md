# Directory hero: align the hero content with the page column (Roman approved)

Page: /bars (the Global Bar Directory), the dark hero with "Discover the World's Best Bars", the subtitle and the three stat tiles.

At about 1000px and below the hero content starts ~36px from the viewport edge while the search box and the filter card below start ~48px (the content container's padding). Give the hero's inner content the same horizontal padding / container as the content below it so the headline, subtitle, stat tiles, search box and filter card share one left edge at every width (390, 768, 1000, 1440). Change only that padding/container rule; the hero height, image, overlay, type sizes and the tiles' layout stay as they are.

Standing layout rule: before/after at 390 and 1440, the alignment as the only difference; report the left-edge px of the headline and the search box at each width. Deploy and report the commit.
